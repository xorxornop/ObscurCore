namespace ObscurCore.Packaging
{
    /// <summary>
    /// Mode controlling length of padding before and after items.
    /// </summary>
    public enum FrameshiftPaddingMode
    {
        FixedLength,
        VariableLength
    }
}
